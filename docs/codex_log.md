# Codex Log

| prompt_id | file_scope | what_codex_generated | edits_made_by_engineer | commit_hash |
|---|---|---|---|---|
| prompt-1 | `up.sh`, `down.sh`, `requirements.txt`, `README.md`, `docs/architecture.md`, repo scaffold | Created the monorepo skeleton, backend package directories, frontend Vite scaffold, data directories, docs stubs, pinned Python requirements, and lifecycle scripts. | Added a minimal FastAPI app plus ingest/index module stubs so `bash up.sh` could start both services on a clean clone; adjusted frontend scaffold manually after latest `create-vite` required a newer Node API than the local runtime. | `e69ce53` |
| prompt-2 | `backend/app/ingest.py`, `backend/tests/test_ingest.py` | Implemented recursive `.txt`/`.md` ingestion, whitespace normalization, 400-word truncation, JSONL export, CLI args, and pytest coverage. | Converted `app.ingest` from a package directory to a module file so `python -m app.ingest --input ... --out ...` resolved exactly; added Python 3.9-compatible `Optional[...]` typing before tests. | `0200956` |
| prompt-3 | `backend/app/index.py`, `backend/app/search/bm25.py`, `backend/app/search/vector.py`, `backend/tests/test_search.py` | Added BM25 pickle/docmap build and query, vector FAISS index build/query, dimension validation, index CLI, and search tests. | Added lazy imports for FAISS and `sentence_transformers`, a NumPy fallback FAISS-compatible test path, and `numpy<2` to avoid `faiss-cpu==1.8.0`/Torch crashes against NumPy 2. | `4d36795` |
| prompt-4 | `backend/app/search/hybrid.py`, `backend/tests/test_hybrid.py`, `docs/decision_log.md` | Implemented `HybridSearcher`, `SearchResult`, min-max and softmax normalization, hybrid score fusion, and hybrid tests. | Added a min-max equal-score guard that returns a uniform distribution so tied BM25/vector scores never produce divide-by-zero or invalid scores. | `1dadb49` |
| prompt-5 | `backend/app/api/routes.py`, `backend/app/db/models.py`, `backend/app/db/session.py`, `backend/tests/test_api.py` | Added SQLAlchemy models, SQLite session/migration setup, API routes for health/search/metrics/logs/experiments, SlowAPI rate limiting, CORS, and API contract tests. | Made the API testable by allowing a fake searcher and test DB session factory through `app.state`; added migration compatibility for existing tables instead of dropping/recreating. | `8a4a41e` |
| prompt-6 | `backend/app/eval.py`, `data/eval/queries.jsonl`, `data/eval/qrels.json`, `backend/tests/test_eval.py`, `data/metrics/experiments.csv` | Added evaluation CLI, 25 labeled queries, curated qrels, nDCG/Recall/MRR metrics, CSV experiment appends, tests, and five alpha experiment runs. | Converted `app.eval` from a package directory into a CLI module and force-added ignored eval/metrics artifacts that are intentionally generated-data paths. | `cd218e1` |
| prompt-7 | `frontend/src/pages/SearchPage.tsx`, `frontend/src/pages/KpiPage.tsx`, `frontend/src/components/ScoreBadge.tsx`, `frontend/src/api/client.ts`, `frontend/vite.config.ts` | Built typed fetch wrappers, Vite `/api` proxy, score badges, debounced search page, KPI page with Recharts, React Router shell, and CSS. | Added `dns.setDefaultResultOrder("ipv4first")` to keep Vite proxying to IPv4 localhost; added explicit React imports in TSX files after the runtime used classic `React.createElement`. | `d1f5e30` |
| prompt-8 | `frontend/src/pages/EvalPage.tsx`, `frontend/src/pages/DebugPage.tsx`, `frontend/src/api/client.ts`, `frontend/src/App.tsx`, `frontend/src/style.css` | Added experiments table/trend chart, debug logs table, severity/date filters, client-side pagination, and typed client methods for `/experiments` and filtered `/logs`. | Replaced placeholder nav routes with real pages; kept pagination client-side so changing pages does not refetch logs. | `460ed1f` |
| prompt-9 | `docs/break_fix_log.md`, `backend/app/search/vector.py`, `backend/app/db/session.py`, `backend/app/search/hybrid.py`, `backend/tests/test_regressions.py` | Added regression tests and break/fix documentation for vector dimension mismatch, SQLite bad schema migration, and min-max divide-by-zero. | Added `QueryLog.user_agent` with server default, bumped schema version to 3, and implemented a SQLite table-rebuild repair for `user_agent TEXT NOT NULL` without a default. | `0c16fa7` |
| prompt-10 | `docs/codex_log.md`, `docs/decision_log.md`, `README.md`, `up.sh` | Finalized documentation with decision rationales, a 9-entry Codex log, README architecture/quickstart/testing/eval/schema/limitations sections, and `up.sh` preflight checks for Python, Node, and npm. | Fixed the shell version comparison helper to call `python3 -` correctly; verified `up.sh` now fails with an actionable Python 3.11 message on the local Python 3.9 host while syntax checks, backend tests, and frontend build pass. | `877ea1a` |
